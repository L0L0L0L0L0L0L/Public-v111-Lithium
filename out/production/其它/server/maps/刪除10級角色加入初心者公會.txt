搜索：

        exp -= getNeededExp();
        level += 1;
        if (level == 11) {
            addGuildMember(1); // 길드 값
            if (this.getGuildId() != 0) {
                dropMessage(5, "[通知]已加入初心者公會。");
            } else {
                dropMessage(5, "[通知]初心者公會已達到最大人数限制。");
            }
        }


替換：

        exp -= getNeededExp();
        level += 1;
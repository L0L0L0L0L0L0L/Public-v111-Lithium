搜索：

        slea.skip((summon.getSkill() == 35111002||summon.getSkill() == 35111009||summon.getSkill() == 35111010) ? 24 : 12); //some pos stuff
        final List<Pair<Integer, Integer>> allDamage = new ArrayList<Pair<Integer, Integer>>();


替換：

        slea.skip((summon.getSkill() == 35111002 || summon.getSkill() == 35111009) ? 24 : 12); //some pos stuff
        final List<Pair<Integer, Integer>> allDamage = new ArrayList<Pair<Integer, Integer>>();